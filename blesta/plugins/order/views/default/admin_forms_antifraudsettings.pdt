            <?php
            // Show module fields
            if (isset($fields_html) && ($antifraud_field_html = $fields_html->generate())) {
                echo $antifraud_field_html;
            }
